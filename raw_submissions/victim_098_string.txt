[FILENAME]: recovered_orphan.txt

**PROBLEM STATEMENT**
Recovered from orphan CPP file.

**CODE**
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        string s;
        cin >> s;

        int n = s.length();
        int answer = 0;

        for (int i = 0; i < n / 2; i++) {
            if (s[i] != s[n - i - 1]) {
                answer += 1;
            }
        }

        cout << answer << endl;
    }

    return 0;
}
```